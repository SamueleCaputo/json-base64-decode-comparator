# json-base64-decode-comparator
Web app to compare two JSON payloads (or base64-encoded JSON), normalize them ignoring order, and show a side-by-side diff with line numbers, missing-field gaps, and synchronized scrolling.
